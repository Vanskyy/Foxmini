services:
  app:
    entrypoint:
    - sleep
    - infinity
    image: fengjy209/foxmini-backend:latest
    init: true
    volumes:
    - type: bind
      source: /var/run/docker.sock
      target: /var/run/docker.sock

